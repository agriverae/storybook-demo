import { Size, Color, Variant } from './types';
declare const StyledButton: import("styled-components").StyledComponent<"button", import("styled-components").DefaultTheme, {
    size: Size;
    color: Color;
    variant: Variant;
}, never>;
export default StyledButton;
