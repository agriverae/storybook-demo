import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { Button } from '../src';

<Meta title="Button" component={Button} />

export const Template = args => <Button {...args} />;

# Button

This is the story regarding our Button component which includes different variant types and colors

<Canvas>
  <Story name='Filled Primary'
    args={{
      text: 'Test',
      size: 'large',
      color: 'primary',
      variant: 'filled',
      disabled: false
    }}>{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story name='Ghost Secondary'
    args={{
      text: 'Test',
      size: 'large',
      color: 'secondary',
      disabled: false,
      variant: 'ghost'
    }}>{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Button} />